import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Divider = () => {
  return (
    <View
      style={{
        alignSelf: "stretch",
        borderColor: "gray",
        borderWidth: 0.3,
        marginHorizontal: 24,
      }}
    />
  );
};

export default Divider;
